# 待办事项

## ✅ 最近完成

- [x] 目录支持子域名 - 通过 `/{domain}/` URL 路由已解决
- [x] 业务域管理 API 完整实现
- [x] 静态文件服务支持多 domain
- [x] 配置自动生成与同步
- [x] **新增页面图谱未重绘** - 已解决
- [x] **图谱边显示优化** - 有向箭头（`showArrows` 配置）
- [x] **虚拟节点处理** - 动态计算，单向链接
- [x] **API 参数验证** - 白名单检查、命令回显
- [x] **reset 参数修复** - 支持 `--settings path` 格式
- [x] **边标签显示** - 显示 frontmatter 字段名，带圆角背景，避免重叠
- [x] **文件夹页分批加载** - `PageList` 默认启用，首批 20 条，每次追加 20 条
- [x] **反向链接分组+分批加载** - 按文件夹分组，阈值内平铺、超阈值分批，子文件夹默认折叠
- [x] **删除业务域 API** - 支持删除配置目录、输入目录、输出目录
- [x] **服务版本号** - 通过 `config.json` 配置，启动时显示版本信息
- [x] **日志路径修复** - `domainBuildHandler` 调用时正确传递日志路径
- [x] **反向链接多级聚合** - 支持文件夹 + frontmatter 字段（date/tags/自定义属性），可配置层级深度和扁平化

## 📋 接下来做什么？

### 🔴 紧急
- [ ] **反向链接增量更新测试** - 验证修改 frontmatter、新增/删除文件后的聚合结果正确性

### 🟡 中期
- [ ] **右侧边栏布局调整** - 局部图谱、目录、反向链接同时显示较拥挤，需要优化布局

## 📋 接下来做什么？

### 方案 A：图谱构建优化（性能主线）
- 构建时预计算局部图谱关联节点
- 按节点 ID 拆分 JSON 文件，减少运行时运算
- **当前状态：已在其他分支完成，等待合并**

### 方案 B：聚合节点（高级功能）
- 按字段（属性、标签、日期）聚合同类节点
- 显示子节点数量，支持展开
- 减少复杂图谱的视觉混乱

### 方案 C：反向链接按属性聚合（扩展）
- 支持配置 `groupBy: ["frontmatter.project", "frontmatter.status"]` 实现多级聚合
- 无对应字段的链接归入"未分类"组
- 保持与文件夹分组一致的 UI 体验（折叠/展开 + 分批加载）

### 方案 D：追踪展示
- 关联节点深度/广度控制
- 详情列表组件（图谱下方展示相关节点）

### 方案 E：目录/文件夹页排序配置
- 支持根据 U3 的 `layout.json` 配置调整排序
- 文件夹页和 Explorer 目录统一排序策略

---

**推荐下一步**：如果局部图谱预计算分支即将合并，可先处理 **方案 E（排序配置）** 或 **方案 C（反向链接属性聚合）**，这两者都是前端体验优化，不涉及构建流程大改。

---

## 现存问题

TODO：

1. ~~前后端配置项不一致~~ - 已解决：移除了 `domain_config.json`，现在只有 `quartz.config.json` 和 `quartz.layout.json`

2. 不设置reset=true时的增量更新不会触发反向链接涉及变更文档的文档的更新。