# 维度系统与动态筛选功能规划

## 背景

来源于用户（汤工）的反馈：当前知识图谱只支持"点击节点跳来跳去"，缺乏按条件筛选和分析的能力。具体诉求：

1. **相对时间/条件过滤**：如"上一年"、"当前时点"筛选数据
2. **变量联动**：多个 `{{变量}}` 共用同一个筛选条件，改一处全局生效，而非每个变量独立写 SQL
3. **变量间计算**：如"总数-正常数=异常数"
4. **字符型维度**：不仅是日期，项目状态、部门等枚举字段同样需要筛选

核心矛盾：项目是**构建时生成**的静态站点，数据在构建时固化，运行时不变。

---

## 核心概念澄清

### 维度 vs 聚合

| | 维度（Dimension） | 聚合（Aggregation） |
|:---|:---|:---|
| **本质** | 定义层：字段 + 值域 | 实例层：维度在具体上下文中的应用 |
| **范围** | 全局的，跨整个域 | 局部的，依附于某个中心节点 |
| **数量** | 少（几个关键字段） | 多（每个局部图谱 × 每个维度值） |
| **示例** | `status` 字段，值域 {进行中, 已完成, 暂停} | 任务A的邻居按status分组 → "进行中"聚合节点(3个) |

**聚合是维度在具体上下文下的体现。**

---

## 功能规划

### P0：文件夹级聚合配置

当前 `quartz.layout.json` 是全局配置，所有实体共用一套聚合规则。不同类型的实体应有不同的可筛选维度。

```json
// 现在：全局
{
  "graph": {
    "aggregation": {
      "rules": [
        { "type": "folder", "depth": 1 },
        { "type": "field", "field": "type" }
      ]
    }
  }
}

// 建议改为：按 folder 配置
{
  "graph": {
    "aggregation": {
      "global": { "rules": [{ "type": "folder", "depth": 1 }] },
      "folders": {
        "tasks": {
          "rules": [
            { "type": "field", "field": "status" },
            { "type": "field", "field": "date", "granularity": "year" }
          ]
        },
        "project": {
          "rules": [
            { "type": "field", "field": "status" },
            { "type": "field", "field": "department" }
          ]
        }
      }
    }
  }
}
```

### P1：维度定义 + 维度值页面生成

#### 维度定义

在 layout 配置中显式声明维度：

```json
"dimensions": [
  { "name": "项目状态", "field": "status", "scope": "project/" },
  { "name": "实施年份", "field": "date", "scope": "project/", "granularity": "year" },
  { "name": "立项月份", "field": "date", "scope": "project/", "granularity": "month" },
  { "name": "所属部门", "field": "department", "scope": "person/,project/" }
]
```

#### 维度值页面（类似 Hugo taxonomy）

一个枚举值一个页面，构建时自动生成：

```
/_dimensions/status/             ← 维度索引页，列出所有枚举值和计数
                                   进行中(15)  已完成(23)  暂停(3)

/_dimensions/status/进行中       ← 维度值页面，列出全域 status=进行中 的所有实体
/_dimensions/status/已完成       ← 维度值页面
/_dimensions/date/2022年         ← 日期维度值页面（按年粒度）
/_dimensions/date/2022年7月      ← 日期维度值页面（按月粒度）
/_dimensions/department/技术部   ← 字符串维度值页面
```

#### 不同字段类型的维度值生成方式

| 字段类型 | 维度值生成 | 页面数量 |
|:---|:---|:---|
| 字符串/枚举 | 直接取 distinct 值 | 少 |
| 日期（granularity=year） | 按年份分组 | 少 |
| 日期（granularity=month） | 按年月分组 | 中等 |
| 数值区间 | 分桶策略 | 取决于分桶 |

#### 维度值页面的图谱数据

维度值页面的图谱 JSON 不能只存匹配节点，还需存关联节点和边：

```
构建时从全局图谱提取子图:
  1. 匹配节点: frontmatter.status === "进行中" 的所有 project
  2. 关联边: 这些 project 参与的所有边
  3. 对端节点: 边的另一头（task, person 等，即使没有 status 字段）
  → 输出为维度值图谱 JSON
```

### P2：URL 参数传递上下文 + 动态筛选

#### 设计思路

聚合节点点击跳转到维度值页面，通过 URL 参数携带上下文：

```
场景1: 直接访问维度值页面（全局视角）
  /_dimensions/status/进行中
  → 图谱: 全域所有 status=进行中 的节点 + 关联边

场景2: 从实体页面点聚合节点跳转（带上下文）
  /_dimensions/status/进行中?context=tasks/MAPTaskSS2026000501
  → 图谱: 先限定全域 status=进行中，再限定跟该任务有关系的
  → 就是该任务的邻居中 status=进行中 的那些
```

一个页面，两种模式，靠 URL 参数区分。

#### 运行时过滤逻辑

```js
// /dimensions/status/进行中?context=tasks/A
const dimGraph = await fetch('/graph/dimension/status/进行中.json')
const contextNode = new URLSearchParams(location.search).get('context')

if (contextNode) {
  // 上下文模式：只保留跟 context 节点有路径的匹配节点
  const contextNeighbors = getContextNeighbors(dimGraph, contextNode)
  filteredNodes = dimGraph.nodes.filter(n => 
    n.id === contextNode || contextNeighbors.has(n.id)
  )
} else {
  // 全局模式：展示所有匹配节点
  filteredNodes = dimGraph.nodes
}
```

### P3：变量支持维度参数

变量从"构建时算好一个数"变为"构建时输出数据集，运行时按条件计算"：

```markdown
---
title: 员工价值
dimensions: [status]   ← 声明本页面接受哪些维度筛选
---

{{sql:count:SELECT count(*) FROM project WHERE status='${dim.status}'}} 个项目
{{sql:sum:SELECT sum(amount) FROM project WHERE status='${dim.status}'}} 万元
```

构建时为每个维度值生成变量数据 JSON（而非固化成数字），运行时按筛选条件取值。

### P4：变量间计算

```markdown
{{expr:total - normal}}  ← 引用其他变量进行计算
```

---

## 文件夹与维度体系的关系

### 文件夹也是一种维度

文件夹分组和字段枚举分组，本质都是**按某个属性把节点归类**：

| | 文件夹维度 | 字段维度 |
|:---|:---|:---|
| 分组依据 | 文件路径（结构性的） | frontmatter 字段（语义性的） |
| 值 | `tasks/`, `project/`, `person/` | `进行中`, `已完成`, `2022年` |
| 特点 | 每个文件必属于一个文件夹（单值） | 可选，可能为空 |
| 作用域 | 全局 | 限定在某个文件夹（scope）下才有意义 |

### 文件夹页 = 文件夹维度值页面

文件夹页（每个目录下的 `index.md`）**已经存在**，它天然就是"文件夹维度值页面"：

```
/project/  (index.md)    ← 这就是"文件夹=project"的维度值页面
/tasks/    (index.md)    ← 这就是"文件夹=tasks"的维度值页面
```

**不需要**再搞一个 `/dimensions/_folder/project/`——那是重复的。

### 文件夹页的局部图谱

文件夹内的文件之间往往没有直接边（如两个 project 各自连不同 task，但 project 之间没连线）。

**建议方案**：展示文件夹内所有节点 + 它们的真实边 + 外部对端节点（灰色/小号提供上下文），不需要虚拟中心节点或人为添加 folder→file 边。文件夹页只是提供了一个"筛选范围"。

### 文件夹页上的维度分类入口

文件夹页可作为维度导航入口，展示该文件夹下可用的维度及计数：

```
/project/ 文件夹页
├── 文件列表（所有 project）
├── 局部图谱
└── 维度分类（跳转入口）
    ├── 按状态: 进行中(5) | 已完成(8) | 暂停(2)
    │     ↓ 点击
    │   /_dimensions/status/进行中?scope=project/
    │
    ├── 按年份: 2022年(3) | 2023年(6) | 2024年(6)
    │     ↓ 点击
    │   /_dimensions/date/2022年?scope=project/
    │
    └── 按部门: 技术部(4) | 市场部(6) | 管理部(5)
          ↓ 点击
        /_dimensions/department/技术部?scope=project/
```

### URL 参数统一设计

| 参数 | 含义 | 来源 | 示例 |
|:---|:---|:---|:---|
| `scope=project/` | 限定文件夹范围 | 文件夹页跳转时携带 | 维度值页面只显示 project/ 下的 |
| `context=tasks/A` | 限定关联范围 | 实体页聚合节点跳转时携带 | 维度值页面只显示跟任务A有关系的 |
| `scope=project/&context=tasks/A` | 两者叠加 | 理论上可叠加 | 任务A关联的 project 中 status=进行中的 |

### 整体导航关系

```
文件夹页 /project/
  ├── 维度分类 → /_dimensions/status/进行中?scope=project/   (带 scope)
  │
实体页 /tasks/MAPTaskSS2026000501
  ├── 聚合节点 → /_dimensions/status/进行中?context=tasks/... (带 context)
  │
维度值页 /_dimensions/status/进行中
  ├── 无参数 → 全局所有 status=进行中 的实体
  ├── ?scope=project/ → project/ 下 status=进行中 的实体
  └── ?context=tasks/A → 跟任务A相关的 status=进行中 的实体
```

---

## 现有聚合节点 vs 维度值页面对比

| | 现有聚合节点 | 维度值页面（新增） |
|:---|:---|:---|
| 用途 | 图谱内的视觉简化 | 内容浏览 + 筛选入口 |
| 作用域 | 局部的，依附于某个中心节点 | 全局的，跨整个域 |
| 存在形式 | 运行时动态生成，无 URL | 构建时生成，有独立 URL |
| 内容 | 只有标签和叶子节点数 | 列出所有匹配实体 + 统计 + 图谱 |
| 可分享 | 否（图谱临时状态） | 是（独立 URL） |
| 变量联动 | 无 | 可做（页面可预计算变量） |

**两者不应合并**：聚合节点解决图谱可视化问题，维度值页面解决内容浏览和筛选问题。但应打通：点击聚合节点跳转到对应的维度值页面（携带 context 参数）。

---

## 技术瓶颈分析

| | 图谱 | 变量（数字统计） |
|:---|:---|:---|
| 维度页面（全局） | 好做，构建时预计算子图 | 好做，构建时按维度值分别跑 SQL |
| 上下文聚合（局部） | 不算难，数据已在 JSON 里，JS 过滤 | **难**，变量是构建时固化的，运行时无法重新查询 |

图谱部分：现有聚合节点已提供基本的维度筛选（不展开无关聚合节点 ≈ 过滤），维度值页面 + context 参数提供更正式的筛选体验。

变量部分：是真正的技术瓶颈。要么构建时为每个实体×维度组合都预计算（组合爆炸），要么构建时输出数据集 JSON 让运行时 JS 计算。

---

## 与现有 bug 的关系

在调试聚合节点展开连线 bug 过程中发现：
- test1 实际用的是 graph3 而非 graph2
- graph3 第2333行 else 分支（所有剩余聚合规则无效时）只加子节点不加连线
- 已修复 graph2 和 graph3 的同位置 bug
- 此 bug 与维度系统规划无关，但调试过程中加深了对聚合机制的理解
