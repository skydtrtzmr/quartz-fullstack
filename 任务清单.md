# 知识图谱功能升级研发项目 — 任务清单

> 来源：`知识图谱功能升级研发项目-任务清单.xlsx`
> 范围：**本清单只做第一阶段（Quartz 静态图谱）**。第二阶段（DeepScope 动态图谱）暂不在本项目范围内。
> 计划完成日期：**2026-09-30**（截至 9-19 约剩 7 个工作日）

---

## 〇、技术前提：v5 迁移合流

第一阶段任务 1-8 均涉及图谱/文件夹页/目录树——正是需移植到 v5 的部分。**铁律：**

1. 新功能一律在 v5（`quartz5/`）上实现，V4（`client/`）冻结，只修致命 bug
2. v5 必须保持独立可用：不启动 Go server 时 `npx quartz build` 裸跑正常（domain 只是通过 per-domain YAML + CLI 参数实现，Quartz 本身不感知 server）
3. 自定义插件源码放 `plugins-local/`，用 `npx quartz plugin add ../plugins-local/xxx` symlink 安装
4. 迁移配置大改（YAML 重写）是所有任务的前置，必须最先完成

---

## 一、第一阶段：Quartz 静态图谱

| # | 任务名称 | 说明 | 责任人 | 人天 | 费用（元） | v5 落点 |
|---|---------|------|--------|------|-----------|---------|
| 1 | 图表代码块渲染 | Quartz 解析 Markdown 正文中的图表代码块并完成图表渲染（ECharts/Vega） | 项目负责人 | 1 | 200 | transformer 插件（本地） |
| 2 | 文件夹页优化 | 文件夹页展示当前目录相关的局部图谱；展示内部动态分类维度（维度值随数据动态变化）；文件列表按映射规则中的排序规则渲染 | 项目负责人 | 2 | 300 | folder-page 改造 + PageType |
| 3 | 聚合维度页 | 图谱中聚合节点可点击跳转到聚合维度页面，页面展示按当前聚合值筛选后的局部图谱；支持上下文传参（仅显示与上一访问节点关联的、按当前聚合值筛选的局部图谱，运行时动态计算） | 项目负责人 | 1 | 200 | PageType `generate` 虚拟页 |
| 4 | 多级动态分类组件 | 文件夹页/目录树/图谱支持多级动态分类；每级可配置多个候选分类字段（来自 U3 映射规则配置），同一时刻每级只选中一个字段作为当前分类轴，用户下拉切换；切换后按新分类路径重新聚合展示（非同级多字段交叉分类） | 项目负责人 | 2 | 300 | 组件插件（本地） |
| 5 | 侧边栏目录树 | 按映射规则中的动态分类维度字段，在文件夹下生成动态分类子文件夹；支持隐藏末级文件（只显示文件夹和动态分类子文件夹）；支持根据多级动态分类刷新目录树结构 | 项目负责人 | 1 | 200 | explorer 改造（explorer-pro） |
| 6 | 阅读空间优化 | 阅读模式隐藏全部侧边栏组件且不留空白占位，正文可拉伸至全宽；阅读区域支持显示上方附件链接、下方附件的 Markdown 文本 | 项目负责人 | 1 | 100 | reader-mode-pro 插件 + Frame |
| 7 | 图谱跳转上下文传参 | 图谱中跳转时传递并记住来源节点，用于筛选后续节点；增加"清除上下文"按钮 | 项目负责人 | 1 | 100 | graph-precompute 插件（localStorage 来源栈方案） |
| 8 | 图谱动态切换分类 | 全局图谱/文件夹页图谱/文件页图谱：按可选动态分类字段，构建时预计算多个版本图谱视图，运行时按用户选择切换渲染 | 项目负责人 | 1 | 200 | graph-precompute 扩展（多版本输出） |
| 9 | U3 图表配置 | U3 配置图表的数据来源、图表类型和样式，嵌在 Markdown 正文部分提供给 Quartz 渲染 | 研发 | 1 | 200 | U3 侧 |
| 10 | U3 变量功能优化 | 支持列表（多行多列）和 CSV 形式变量配置，导入 Markdown 正文区域；简易循环处理（Markdown 表格、逐行无序列表、指定字段加标题符号）；替换变量生成 Markdown 时做语法转义 | 研发 | 2 | 400 | U3 侧 |
| 11 | U3 映射规则配置 | 指定附件链接字段（附件转 Markdown 存库，数据源配置供 Quartz 渲染）；指定动态分类维度字段（支持优先级，时间日期支持粒度：月/年）；指定排序字段（目录树、文件夹页列表排序）；同步简化原"业务域配置"（每目录限 1 条映射规则） | 研发 | 2 | 200 | U3 侧 |
| 12 | 第一阶段联调测试 | 上述功能联调测试 | 项目负责人、研发 | 1 | 100 | — |
| 13 | 第一阶段使用文档 | Quartz 静态图谱支持的功能及常见操作说明 | 项目负责人 | 1 | 100 | — |

**第一阶段小计：17 人天，¥2600，计划完成 2026-09-30**

### 与旧版清单（Q/D/UQ 编号）的映射

| 新任务 | 对应旧编号 |
|--------|-----------|
| 1 图表代码块渲染 | Q6-5 |
| 2 文件夹页优化 | Q2 + Q14 + Q17 |
| 3 聚合维度页 | Q15 + Q16 + Q18 |
| 4 多级动态分类组件 | Q13（演进：同级单选轴） |
| 5 侧边栏目录树 | Q14（目录树部分）+ Q5 |
| 6 阅读空间优化 | Q6-3 + Q6-1（附件嵌入部分） |
| 7 图谱跳转上下文传参 | 新增（原 D7 上下文过滤的静态图谱版） |
| 8 图谱动态切换分类 | 新增 |
| 9-11 U3 侧 | UQ1/UQ2 + 图表配置助手 + 新增变量/附件能力 |
| 旧 Q6-8（文档关联展示）、Q19/Q20（变量联动计算） | 未进本版清单，后置 |

---

## 二、第二阶段：DeepScope 动态图谱（⚠️ 本项目不做，仅存档）

> 以下内容仅从 Excel 存档备查，**不在当前项目范围内**：U3 主体建模与 ER 图（2 人天）、节点与边数据服务（2）、动态图谱交互（2）、聚合节点与聚合计算（2）、U3 模块与图谱间跳转（3）、联调测试（2）、使用文档（1）。小计 14 人天 / ¥2400，暂定 2026-11-30。

---

## 三、执行顺序（结合 v5 迁移）

| 顺序 | 内容 | 说明 |
|------|------|------|
| **第 0 步（前置）✅ 2026-09-19 完成** | v5 配置大改 | 已重写 `quartz.config.yaml`（oceanColors/zh-CN/插件对齐/布局对齐）；验收通过：20006 文件 3 分钟构建 0 报错、主题色与中文 UI 生效、内容页左右栏组件齐全、folder/tag 页右侧正确排除、baseUrl 子路径生效。期间修复两个问题：① 根目录 `@quartz-community/utils` 降级为 0.1.0 缺 `normalizeHastElement` 导出（已升 1.0.1）② 移植 v4 glob 补丁（`gitignore: false`，否则被仓库根 `.gitignore` 的 `/input` 规则清零） |
| **第 1 步（验证件）** | 任务 6：阅读空间优化 | 最小任务，同时验证"本地插件 → symlink → 构建 → 渲染"全链路和 Frame 体系 |
| 第 2 步 | 任务 7：图谱跳转上下文传参 | graph-precompute 插件骨架 + graph3 移植的载体 |
| 第 3 步 | 任务 5：侧边栏目录树 | explorer-pro 插件 |
| 第 4 步 | 任务 2 + 3 + 4 + 8 | 文件夹页/维度页/多级分类/图谱多版本（相互耦合，成组做） |
| 并行 | 任务 9-11（U3 侧） | 不依赖 v5 迁移，随时开工 |
| 收尾 | 任务 12 + 13 | 联调 + 文档 |

## 四、第 0 步验收标准（v5 配置大改）

1. **裸跑**：不启动 Go server，`npx quartz build -d ../input/demo-bench -o ../output/demo-bench` 成功，0 报错
2. **主题/语言**：输出页面 oceanColors 配色生效、界面中文（zh-CN）
3. **布局**：左右栏组件位置与 v4 输出一致（Explorer 左、Graph/Backlinks 右、toolbar 组）
4. **子路径**：baseUrl 带 `/demo-bench` 子路径时，静态资源与图谱 JSON fetch 路径前缀正确，不 404

---

## 五、滚动进度（截至 2026-09-20）

> 本节为项目内部滚动更新，原始任务表（§一）保持不变。

| 项 | 状态 | 落点 / 证据 |
|----|------|------------|
| 第 0 步 v5 配置大改 | ✅ 2026-09-19 | 见 §四 验收标准，已通过 |
| 任务 6 阅读空间优化 | 🟡 主体完成 | reader-mode-pro：按钮 `position:fixed` 视口右上角；与全局图谱、夜间模式组成「右上角按钮排」（`right: 1rem / 3.25rem / 5.5rem`，均 32×32）；开启后左右栏 `display:none` 且不留占位、正文全宽；`::root`→`:root` 修正（BUG-COM-001）；与全局图谱浮层冲突修复（BUG-COM-003，`readermode.scss` 末段 `:has(.global-graph-outer.active)`）。**未完成**：正文区显示「上方附件链接 / 下方附件 Markdown 文本」（需 Frame + 附件字段） |
| 任务 5 侧边栏目录树 | 🟡 基线完成 | explorer-pro 已移植 v4 Explorer2 引擎（`src/util/fileTrie.ts` 扁平化 + 虚拟滚动 + frontmatter 排序 + `hideFiles`）。**未完成**：按动态分类维度生成子文件夹、多级刷新——落点 `explorer-pro.inline.ts:1000-1030`（建树管线）/ `fileTrie.ts:77-106`（插入）/ `:193-297`（渲染） |
| 任务 7 图谱跳转上下文传参 | 🟡 部分 | graph-pro 已接入 v4 交互层（局部/全局图谱、大区模式、聚合徽标、预计算）。**未完成**：localStorage 来源栈 + 「清除上下文」按钮 |
| 任务 8 图谱动态切换分类 | 🟡 部分 | 聚合规则（folder/field/date）+ 构建期预计算已通：`graph/local/{djb2[0:2]}/{djb2[2:4]}/{slug}.json`、`graph/global/graphGlobal.json`。**未完成**：「按分类字段预计算多版本视图 + 运行时切换」 |
| 任务 2 / 3 / 4（文件夹页 / 聚合维度页 / 多级动态分类） | ⬜ 未开始 | `folder-page` 仍为社区版（无本地 fork）；相关构想见 `维度系统与动态筛选功能规划.md` |
| 依赖外链整改（CDN → 本地） | ✅ 2026-09-20 实测 | 打开全局图谱后 38 个资源全为本站、**站外请求 0**、jsdelivr 命中 0；d3@7 / pixi.js@8 已随 graph-pro 打包（`static/scripts/script-3-*.js`，1.27MB）。**残留（死代码）**：pixi 内置 basis/ktx 转码器默认 URL 仍指向 jsdelivr（脚本内字符串，仅当加载 `.basis/.ktx2` 纹理时才会请求，本站不涉及） |
| 白屏/卡顿排查 | ✅ 已收口 | 服务端与产物字节级无问题（15 个关键文件逐一 md5 比对、gzip/chunked/keep-alive 正常、32 个资源全 200）；关闭 graph-pro 重建后用户侧恢复正常 → 与 graph-pro 开启时的构建产物强相关（候选链路：`static/metadata.json` 的 `lastBuildTime` 变更 → explorer-pro 的 sessionStorage 失效路径）。详见 `.codebuddy/memory/2026-09-19.md`、`2026-09-20.md` |
| 右上角按钮排统一 | ✅ 2026-09-20 | 阅读模式 / 全局图谱 / 夜间模式三个按钮同级等距；夜间模式由左侧工具栏移入（YAML `position: header` + `quartz/styles/custom.scss` 覆盖），详见 `QUARTZ5-COMMANDS.md` |
| v5 `--settings`（指定任意配置文件） | ✅ 2026-09-21 | `quartz/plugins/loader/config-loader.ts` 的 `resolveConfigPath()` + `quartz/cli/args.js` 注册参数；`--settings <目录\|文件>`（含 `=` 形式）整份取代主 YAML，路径非法则告警回退；实测通过。域 YAML 的 `aggregation` 段（文件夹粒度聚合规则）**尚未消费**，graph-pro 解析留待后续 |
| 虚拟节点占位页（v4 `VirtualNodePage` 迁移） | ✅ 2026-09-21 | 新建 `plugins-local/virtual-node-pro`（**pageType 插件 + `generate()`**，布局键 `virtual-node`）；实测 9/9 占位页与 v4 `output/demo-region/static/virtualNodeIndex.json` 逐条一致（标题、说明文案、引用来源数量）；右栏含局部图谱 + Backlinks、不含阅读模式；占位页进 `contentIndex.json`（预期行为）。**已知副作用（用户暂不处理）**：占位节点会出现在左侧目录树与搜索结果里；图谱端未做"虚拟节点"特殊区分 |

---

## 六、聚合/分类规则外置

> **方案讨论中，未定稿**：与用户讨论确定后再写入本节（约定：方案先讨论、不直接落文档）。

---

## 七、遗留小项（低优先级）

1. pixi 内置 basis/ktx 转码器的 jsdelivr 默认 URL（死代码，仅当加载 `.basis/.ktx2` 纹理才请求）—— 可选清理
2. Go server 构建命令仍指向 v4 client（`server/config.json:14-21`）—— v5 上线切换是独立议题，先不动
3. `server/static.go` 两个已知小 bug（`.html/.json` 的 Content-Type 一律被强制 `text/html`；子目录无尾斜杠不补 `/`）—— 待用户发话再修
4. **`output/` 与 `settings/` 下的目录名要区分 v4 / v5**（用户 2026-09-21 提出）—— 现状：`output/demo-region` 已被 v4 构建覆盖、v5 产物散在 `output/demo-region-v5-*`、`output/demo-region-prev*`；后续统一整理命名与清理旧目录
