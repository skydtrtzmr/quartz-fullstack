

## 迁移到 v5 是否值得：详细分析

### 一、v4 → v5 的核心架构差异

| 维度 | v4（当前） | v5 |
|------|-----------|-----|
| **配置格式** | `quartz.config.ts`（TypeScript 编程式） | `quartz.config.yaml`（声明式） |
| **插件管理** | 内置 + 代码里 import | 独立安装（`npx quartz plugin install`） |
| **构建引擎** | `parseMarkdown` workerpool 多线程 | **完全一样**的代码和并发策略 |
| **Layout** | `quartz.layout.ts`（TypeScript 编程式） | YAML 中 `layout:` 字段（声明式） |
| **组件系统** | 组件直接在 `quartz/components/` 目录里 | 组件随插件安装，在 `.quartz/plugins/` 里 |

### 二、你项目中的自定义部分（迁移难点）

你 v4 的 `client/` 目录里有大量深度定制：

1. **GraphGlobal / GraphLocal emitter** — 构建时预计算图谱，这是你自己写的，v5 社区版 graph 插件没有这个功能
2. **Explorer2** — 自定义的文件树浏览器（带 OverflowList、排序等），v5 社区版 explorer 是不同的实现
3. **Search2** — 自定义搜索组件
4. **ReaderMode** — 自定义阅读模式
5. **FrontmatterMeta** — 自定义 frontmatter 展示
6. **ConditionalRender** — 条件渲染包装器
7. **VirtualNodePage** — 虚拟节点页面（图谱节点详情页）
8. **oceanColors 主题** — 自定义配色
9. **`--settings` 运行时覆盖** — 通过 JSON 文件动态覆盖配置，整个 Go server 层依赖这个
10. **aggregation / coreNodeFilter / regionRules** — 图谱的高级聚合和区域规则系统

这些自定义功能**大部分没有对应的 v5 社区插件**。v5 的社区插件是基础版（标准 graph、标准 explorer、标准 backlinks），不具备你深度定制的能力。

### 三、迁移工作量评估

| 工作项 | 难度 | 说明 |
|--------|------|------|
| 基础配置迁移（config → yaml） | 低 | 简单重写 |
| Layout 迁移 | 中 | 需要理解 v5 的 layout 声明式语法 |
| 安装社区插件替代标准功能 | 低 | alias-redirects, content-index 等有现成的 |
| **GraphGlobal/GraphLocal 迁移** | **高** | v5 社区 graph 插件不支持构建时预计算，需要自己写 v5 版插件或 fork 社区插件 |
| **Explorer2 迁移** | **高** | v5 社区 explorer 实现不同，你的排序/OverflowList 等需要适配 |
| **Search2 迁移** | **中** | v5 有 search 插件，但功能差异需评估 |
| **VirtualNodePage 迁移** | **中** | v5 有 PageType 系统（bases-page 等），概念接近但 API 不同 |
| **`--settings` 运行时覆盖** | **高** | v5 用 yaml 配置，没有 TS 编程能力，需要另外设计动态配置方案 |
| **Go Server 适配** | **中** | 命令行参数变化，`--settings` 需要重新设计 |
| **oceanColors 主题** | **低** | yaml 里直接写颜色 |

**总估计：2-3 周全职工作量**，其中大部分时间花在 GraphGlobal/Explorer2 这两个深度定制组件上。

### 四、迁移的实际收益

| 收益 | 影响程度 |
|------|---------|
| **插件独立管理，上游更新更容易** | 中 — 但你深度定制的部分不会用社区版 |
| **声明式配置更清晰** | 低 — 你目前的 TS 配置已经够用，且更灵活 |
| **构建性能提升** | **无** — parse 阶段代码完全一样 |
| **增量构建（watch 模式）** | 低 — v5 的增量构建和 v4 一样，你已经有 Go server 按需重建 |
| **未来 v5 生态发展** | 中 — 长期看 v5 会是主线 |

### 五、我的建议

**当前不建议迁移**。理由：

1. **构建性能没有提升** — 你最关心的 2 万文件构建速度，v4 和 v5 的 parse 代码完全一样
2. **你的深度定制在 v5 上需要重写** — GraphGlobal 预计算、Explorer2、运行时配置覆盖，这些是你项目的核心竞争力，v5 社区插件覆盖不了
3. **v5 插件系统在 Windows 上不稳定** — 之前我们遇到的并行构建崩溃问题，说明 v5 的插件安装流程还不成熟
4. **你的 `--settings` JSON 覆盖机制**在 v5 的纯 yaml 配置下需要完全重新设计
5. **迁移成本高，收益低** — 2-3 周工作量换来的是一个架构不同但构建速度一样的系统

**真正能提升构建速度的方向**反而是你之前在 v4 上做的：
- SQLite 增量构建（只重建变化的文件）
- 提高 workerpool 并发上限（改 `clamp` 的 max 从 4 到 8-16）
- emit 阶段并行化（v5 做了，v4 也可以做）

如果 v5 未来社区插件生态成熟了（特别是 graph 预计算、高级 explorer 出现社区版），再考虑迁移不迟。